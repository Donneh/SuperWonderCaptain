ffrom tkinter import *

window = Tk()
Label(text="Hints nodig?").pack()

def close_window():
    window.destroy()

frame = Frame(window)
frame.pack()
button = Button(frame, text="Klik hier!", command=close_window)
button.pack()

window.mainloop()

master = Tk()
Label(text="Wil je een hint waarbij je 3 punten verliest?").pack()

separator = Frame(height=2, bd=1, relief=SUNKEN)
separator.pack(fill=X, padx=5, pady=5)

c = Button(master, text="Hint (-3 punten)", command=close_window)
c.pack()

mainloop()
